// helper functions

// prend en entrée une chaîne
// renvoie en sortie cette même chaîne
// avec la première lettre en majuscule
function capitalize(str) {
    return str.charAt(0).toUpperCase() + str.slice(1);
}